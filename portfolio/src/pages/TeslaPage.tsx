import React from 'react';
import Tesla from '../container/Tesla';
import { withRouter } from 'react-router-dom';

const TeslaPage = () => {
  return <Tesla />;
};

export default withRouter(TeslaPage);
